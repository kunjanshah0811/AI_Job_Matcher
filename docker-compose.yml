version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8501}:${PORT:-8501}"
    environment:
      - PORT=${PORT:-8501}
    restart: always